package com.bhimz.simpleweather.domain.service

import com.bhimz.simpleweather.domain.model.Weather

class WeatherService {
    fun getWeather(cityName: String): List<Weather>? {
        return emptyList()
    }
}